# GLM-5.3 Flash v10 DCP2 — Loom planning benchmark — 2026-08-31

## Run identity

- Run: `20260831T122019Z-glm-v10-dcp2-1`
- Arm: `glm-v10-dcp2`
- Model selector: `desktop-vllm/glm-5.3-flash-exl3-k4-vision-fp8kv-mtp-359k-v10:max`
- Runtime image: `sha256:ef5f2fcb25d16abdcd800ff70b158e077780f2cb550a0ebf5bd1fe12e9f44553`
- Loom base: `48483ba25ccacd87cb386df7d33f177573de99f1`
- Benchmark commit: `bac1ba79bf73a1dac0bec62174fe093a3e3c2ce7`
- Protocol: v2, `f8d3a66edae61265db15492d5508f1d3e6ac216d59b23519a8c5aff5daa1ce99`
- Context consumed by parent at stop: 31.6% of 359K

This was one requested, unblinded repetition. Do not treat it as a three-run arm result or compare a single score as a winner declaration.

## Mechanical result

**PASS.** The run reached the exact planning-only boundary:

- brainstorm, spec, plan, alignment, and task graph present;
- `current_phase=execute`, `current_wave=1`;
- all nine tasks pending, `executing_tasks=[]`;
- no implementation/review/test/ADR/wave-gate child launched;
- frozen files byte-identical;
- no production or test file changed;
- all five planning children attested to the exact v10 selector;
- no Cortex recall in the transcript;
- v10 container stayed healthy with restart count zero.

## Semantic grade

**69/104 (66.3%)**

| Group | Score |
|---|---:|
| Specification | 15/20 |
| Interview | 6/12 |
| Architecture | 22/28 |
| Plan alignment | 5/12 |
| Decomposition | 12/20 |
| Process | 9/12 |

Requirement discovery:

- 23/26 hidden FRs fully discovered in spec, plan, and graph;
- 3/26 partial at every layer: FR-039, FR-040, FR-041;
- zero fully absent FRs.

The complete citation-based score and checklist are retained locally in:

- `benchmarks/loom-model-ab/runs/20260831T122019Z-glm-v10-dcp2-1/rubric-score.md`
- `benchmarks/loom-model-ab/runs/20260831T122019Z-glm-v10-dcp2-1/discovery-checklist.tsv`

## Strongest result

The plan cleanly separated framing, hostile parsing, raw Pi envelopes, parsed ADTs, correlation state, and shell effects. Its correlation branch table preserved exact request ids, mismatch behavior, cancellation, duplicate suppression, and settled no-ops in implementation-usable form.

## Decisive defect

The plan explicitly said an `agent_settled` event with pending dialogs retained `pending`; it also omitted pending clearing for `child-exited` and `parent-abort`. Protocol v2 requires all three terminal transitions to clear pending. The error propagated from spec to plan to T2, and plan alignment incorrectly reported zero gaps.

The graph also expanded six local architecture choices into six final-wave ADR tasks outside the frozen two-file implementation scope. That reduced interview economy, alignment, and decomposition scores.

## Loom defects observed and repaired after grading

The preserved run exposed infrastructure friction kept separate from model quality:

1. The first decompose spawn retained `{spec_file_path}` / `{plan_file_path}` placeholders from the prompt template's JSON example and was blocked before retry.
2. Read-only guarded-directory probes using `2>/dev/null` were falsely classified as writes.
3. Phase 4d instructed the parent to create `.loom/verification-manifest.json`, while guard-state-file and block-direct-edits blocked both direct write paths.
4. The parent attempted `jq` on JSONC `tsconfig.json` and recovered; this was model/tool use rather than a committed Loom defect.

Loom commit `3de455c` fixed the first three infrastructure defects:

- decompose examples no longer carry live template placeholders;
- exact `2>/dev/null` is allowed while writable/lookalike redirects remain blocked;
- a typed `write-verification-manifest` helper validates and installs operator authority through a no-follow, no-overwrite seam before Task population.

Validation at that commit: 5,656/5,656 tests, clean typecheck/unused-code gates, and clean project lint.

Cross-model interpretation: [`2026-08-31-glm-v10-vs-gpt-5.6-sol-analysis.md`](2026-08-31-glm-v10-vs-gpt-5.6-sol-analysis.md).
