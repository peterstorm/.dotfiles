# GLM-5.3 Flash v10 DCP2 — qualification receipt — 2026-08-31

## Identity

- Image: `sha256:ef5f2fcb25d16abdcd800ff70b158e077780f2cb550a0ebf5bd1fe12e9f44553`
- Served model: `glm-5.3-flash-exl3-k4-vision-fp8kv-mtp-359k-v10`
- Hardware: 2× RTX PRO 6000 Blackwell Workstation Edition, 450 W
- Runtime: TP2 / EP2 / DCP2 (`ag_rs`), FP8 DS-MLA KV, built-in greedy MTP3, prefix cache, vision
- Graphs: FULL + PIECEWISE with capture sizes `1,2,3,4,8,12,16`
- Restart policy: `no`

The image verifier passed the complete GLM_NEXT FP8 cache ABI: packed 528-byte logical records, 656-byte physical token stride, page-boundary writes, untouched padding, CUDA graph capture/replay, decode, extend, CKV gather, replicated Mamba state, exact top-k, overlap-safe state copies, bounded slots, and graph-stable KPool addresses.

## Runtime-headroom repair

A profiler-derived warm allocation exposed 4.92 GiB/GPU of KV and 971,411 logical tokens, but left only about 102 MiB free. The first 2,048-token DCP2 prefill chunk of a 22,418-token request required a 128 MiB query all-gather and failed with CUDA OOM.

The launcher now fixes `--kv-cache-memory-bytes=3758096384` (3.5 GiB/GPU). This removes warm/cold profiler variance and preserves runtime collective headroom.

| Measurement | Cold cache | Warm restart |
|---|---:|---:|
| KV budget | 3.5 GiB/GPU | 3.5 GiB/GPU |
| Logical KV tokens | 691,602 | 691,602 |
| 359K concurrency | 1.93× | 1.93× |
| Free memory immediately after startup | 776–823 MiB | 2,168–2,195 MiB |
| Restart count | 0 | 0 |

The exact former crash request then passed twice: 22,418 prompt tokens completed in 11.72 s cold at the request level and 1.01 s from the aligned cache, with 17,280 cached tokens. The service retained 1.42–1.45 GiB/GPU after runtime buffers had been exercised.

## Correctness and capabilities

- Authenticated text: exact `TEXT_OK`.
- Determinism: ten identical `temperature=0`, `top_p=1`, `seed=0` requests produced one complete-message SHA-256 hash, `fc979579d99f52eb21cd4acef9f9b288bfc4f33e8722b3149b5c63b11509af55`.
- Vision: generated 32×32 red PNG returned exact `red`; 16 image tokens.
- MTP3: draft and accepted-token counters increased; no ReplaySSM or DFlash fallback.
- Shared prefix: three concurrent 127,937-token requests each reused 124,416 tokens and returned their distinct expected values.
- Mixed traffic: an exact 256K retrieval passed while a 2,048-token decode and two short requests overlapped.

### Exact long-context retrieval

| Prompt tokens | Result | Wall time |
|---:|---|---:|
| 128,000 | exact buried code | 23.93 s |
| 261,900 | exact buried code | 44.66 s |
| 358,900 | exact buried code | 75.11 s |

## Throughput

The existing shell harness counts SSE chunks and under-reports multi-token deltas. Rates below use API `usage.completion_tokens` and measured wall/TTFT times.

### Normal C1

| Run | Output tokens | Wall | Usage-derived output tok/s | TTFT |
|---:|---:|---:|---:|---:|
| 1 | 800 | 6.181 s | 129.4 | 195 ms |
| 2 | 800 | 6.369 s | 125.6 | 119 ms |
| 3 | 800 | 6.464 s | 123.8 | 119 ms |

Median normal C1 decode: **125.6 output tok/s**.

### Long-context cells

| Prompt | Output | TTFT | Decode after first token | Overall including prefill |
|---:|---:|---:|---:|---:|
| 32,000 | 512 | 5.97 s | 118.6 tok/s | 49.8 tok/s |
| 128,000 | 512 | 23.94 s | 120.5 tok/s | 18.2 tok/s |
| 256,000 | 512 | 54.59 s | 115.8 tok/s | 8.7 tok/s |

## Sustained soak

A 30-minute four-sequence soak continuously combined one unique ~32K prefill/128-token decode stream with three 256-token decode streams.

- 661 successful requests
- 6,970,868 prompt tokens
- 141,440 completion tokens
- zero HTTP or completion-contract errors
- zero container restarts
- zero fatal logs, CUDA OOMs, illegal-memory errors, or EngineCore failures
- zero Xid, SXid, or MMU faults
- final state idle with no running or waiting requests

Two earlier soak harness attempts were discarded as harness false negatives: they required an exact short retrieval string while allowing the model to consume the output budget explaining the answer. The final stability soak removed semantic retrieval from its contract because exact retrieval had already passed independently at 128K, 261.9K, and 358.9K.

## Qualification decision

**Benchmark-ready.** The candidate is suitable for Loom/model benchmark runs. It is not automatically production-promoted: cross-profile DCP1/DCP2 output comparison and any separate release-policy gate remain orthogonal.
