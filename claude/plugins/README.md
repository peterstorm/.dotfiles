# Claude Plugins

Plugin source and marketplace have moved to `~/dev/claude-plugins/`.
See that repo's README for setup and usage instructions.

This directory in dotfiles is no longer the marketplace location.
