apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: cloudfare-api-token-secret
spec:
  refreshInterval: 0h
  secretStoreRef:
    kind: ClusterSecretStore
    name: azure-store
  target:
    name: cloudfare-api-token-secret
  data:
    - secretKey: cloudfare-api-token-secret
      remoteRef:
        key: api-token
