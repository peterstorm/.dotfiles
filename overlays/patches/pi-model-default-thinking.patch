diff -ruN a/dist/core/agent-session.d.ts b/dist/core/agent-session.d.ts
--- a/dist/core/agent-session.d.ts	1970-01-01 00:00:01.000000000 +0000
+++ b/dist/core/agent-session.d.ts	2026-08-12 10:08:38.903745441 +0000
@@ -15,6 +15,25 @@
 import type { Agent, AgentEvent, AgentMessage, AgentState, AgentTool, ThinkingLevel } from "@earendil-works/pi-agent-core";
 import type { ImageContent, Model, TextContent } from "@earendil-works/pi-ai/compat";
 import { type BashResult } from "./bash-executor.ts";
+export type ThinkingPreference = {
+    kind: "baseline";
+    level: ThinkingLevel;
+} | {
+    kind: "pinned";
+    level: ThinkingLevel;
+};
+export type ThinkingTransition = {
+    kind: "user";
+} | {
+    kind: "automatic";
+    persistDefault: boolean;
+};
+export type ResolvedThinkingTransition = {
+    level: ThinkingLevel;
+    persistDefault: boolean;
+    source: "scoped" | "pinned" | "model-default" | "baseline";
+};
+export declare function resolveThinkingTransition(preference: ThinkingPreference, scopedLevel: ThinkingLevel | undefined, modelDefault: ThinkingLevel | undefined, baselineLevel: ThinkingLevel): ResolvedThinkingTransition;
 import { type CompactionResult } from "./compaction/index.ts";
 import { type ContextUsage, type ExtensionCommandContextActions, type ExtensionErrorListener, type ExtensionMode, ExtensionRunner, type ExtensionUIContext, type InputSource, type ReplacedSessionContext, type SessionStartEvent, type ShutdownHandler, type ToolDefinition, type ToolInfo } from "./extensions/index.ts";
 import type { CustomMessage } from "./messages.ts";
@@ -116,6 +135,10 @@
         model: Model<any>;
         thinkingLevel?: ThinkingLevel;
     }>;
+    /** Explicit unclamped session thinking preference or baseline. */
+    thinkingPreference: ThinkingPreference;
+    /** Baseline preference restored when leaving a model-local default. */
+    preferredThinkingLevel: ThinkingLevel;
     /** Resource loader for extensions, skills, prompts, themes, context files, and system prompt */
     resourceLoader: ResourceLoader;
     /** SDK custom tools registered outside extensions */
@@ -460,7 +483,7 @@
      * Clamps to model capabilities based on available thinking levels.
      * Saves to session and settings only if the level actually changes.
      */
-    setThinkingLevel(level: ThinkingLevel): void;
+    setThinkingLevel(level: ThinkingLevel, transition?: ThinkingTransition): void;
     /**
      * Cycle to next thinking level.
      * @returns New level, or undefined if model doesn't support thinking
diff -ruN a/dist/core/agent-session.js b/dist/core/agent-session.js
--- a/dist/core/agent-session.js	1970-01-01 00:00:01.000000000 +0000
+++ b/dist/core/agent-session.js	2026-08-12 10:00:39.774838247 +0000
@@ -24,6 +24,14 @@
 import { executeBashWithOperations } from "./bash-executor.js";
 import { calculateContextTokens, collectEntriesForBranchSummary, compact, estimateContextTokens, estimateTokens, generateBranchSummary, prepareCompaction, shouldCompact, } from "./compaction/index.js";
 import { DEFAULT_THINKING_LEVEL } from "./defaults.js";
+
+/** Pure precedence rule for model-switch thinking state. */
+export function resolveThinkingTransition(preference, scopedLevel, modelDefault, baselineLevel) {
+    if (scopedLevel !== undefined) return { level: scopedLevel, persistDefault: true, source: "scoped" };
+    if (preference.kind === "pinned") return { level: preference.level, persistDefault: true, source: "pinned" };
+    if (modelDefault !== undefined) return { level: modelDefault, persistDefault: false, source: "model-default" };
+    return { level: baselineLevel, persistDefault: true, source: "baseline" };
+}
 import { exportSessionToHtml } from "./export-html/index.js";
 import { createToolHtmlRenderer } from "./export-html/tool-renderer.js";
 import { ExtensionRunner, wrapRegisteredTools, } from "./extensions/index.js";
@@ -125,6 +133,8 @@
     _extensionErrorListener;
     _extensionErrorUnsubscriber;
     _modelRuntime;
+    _thinkingPreference;
+    _preferredThinkingLevel;
     // Tool registry for extension getTools/setTools
     _toolRegistry = new Map();
     _toolDefinitions = new Map();
@@ -139,6 +149,8 @@
         this.sessionManager = config.sessionManager;
         this.settingsManager = config.settingsManager;
         this._scopedModels = config.scopedModels ?? [];
+        this._thinkingPreference = config.thinkingPreference;
+        this._preferredThinkingLevel = config.preferredThinkingLevel;
         this._resourceLoader = config.resourceLoader;
         this._customTools = config.customTools ?? [];
         this._cwd = config.cwd;
@@ -1196,12 +1208,13 @@
             throw new Error(`No API key for ${model.provider}/${model.id}`);
         }
         const previousModel = this.model;
-        const thinkingLevel = this._getThinkingLevelForModelSwitch();
+        const transition = resolveThinkingTransition(this._thinkingPreference, undefined, model.defaultThinkingLevel, this._preferredThinkingLevel);
+        const thinkingLevel = transition.level;
         this.agent.state.model = model;
         this.sessionManager.appendModelChange(model.provider, model.id);
         this.settingsManager.setDefaultModelAndProvider(model.provider, model.id);
         // Re-clamp thinking level for new model's capabilities
-        this.setThinkingLevel(thinkingLevel);
+        this.setThinkingLevel(thinkingLevel, { kind: "automatic", persistDefault: transition.persistDefault });
         await this._emitModelSelect(model, previousModel, "set");
     }
     /**
@@ -1231,7 +1244,8 @@
         const len = scopedModels.length;
         const nextIndex = direction === "forward" ? (currentIndex + 1) % len : (currentIndex - 1 + len) % len;
         const next = scopedModels[nextIndex];
-        const thinkingLevel = this._getThinkingLevelForModelSwitch(next.thinkingLevel);
+        const transition = resolveThinkingTransition(this._thinkingPreference, next.thinkingLevel, next.model.defaultThinkingLevel, this._preferredThinkingLevel);
+        const thinkingLevel = transition.level;
         // Apply model
         this.agent.state.model = next.model;
         this.sessionManager.appendModelChange(next.model.provider, next.model.id);
@@ -1240,7 +1254,7 @@
         // - Explicit scoped model thinking level overrides current session level
         // - Undefined scoped model thinking level inherits the current session preference
         // setThinkingLevel clamps to model capabilities.
-        this.setThinkingLevel(thinkingLevel);
+        this.setThinkingLevel(thinkingLevel, { kind: "automatic", persistDefault: transition.persistDefault });
         await this._emitModelSelect(next.model, currentModel, "cycle");
         return { model: next.model, thinkingLevel: this.thinkingLevel, isScoped: true };
     }
@@ -1255,12 +1269,13 @@
         const len = availableModels.length;
         const nextIndex = direction === "forward" ? (currentIndex + 1) % len : (currentIndex - 1 + len) % len;
         const nextModel = availableModels[nextIndex];
-        const thinkingLevel = this._getThinkingLevelForModelSwitch();
+        const transition = resolveThinkingTransition(this._thinkingPreference, undefined, nextModel.defaultThinkingLevel, this._preferredThinkingLevel);
+        const thinkingLevel = transition.level;
         this.agent.state.model = nextModel;
         this.sessionManager.appendModelChange(nextModel.provider, nextModel.id);
         this.settingsManager.setDefaultModelAndProvider(nextModel.provider, nextModel.id);
         // Re-clamp thinking level for new model's capabilities
-        this.setThinkingLevel(thinkingLevel);
+        this.setThinkingLevel(thinkingLevel, { kind: "automatic", persistDefault: transition.persistDefault });
         await this._emitModelSelect(nextModel, currentModel, "cycle");
         return { model: nextModel, thinkingLevel: this.thinkingLevel, isScoped: false };
     }
@@ -1272,16 +1287,20 @@
      * Clamps to model capabilities based on available thinking levels.
      * Saves to session and settings only if the level actually changes.
      */
-    setThinkingLevel(level) {
+    setThinkingLevel(level, transition = { kind: "user" }) {
         const availableLevels = this.getAvailableThinkingLevels();
         const effectiveLevel = availableLevels.includes(level) ? level : this._clampThinkingLevel(level, availableLevels);
         // Only persist if actually changing
         const previousLevel = this.agent.state.thinkingLevel;
         const isChanging = effectiveLevel !== previousLevel;
         this.agent.state.thinkingLevel = effectiveLevel;
+        if (transition.kind === "user") {
+            this._thinkingPreference = { kind: "pinned", level };
+            this._preferredThinkingLevel = level;
+        }
         if (isChanging) {
             this.sessionManager.appendThinkingLevelChange(effectiveLevel);
-            if (this.supportsThinking() || effectiveLevel !== "off") {
+            if ((transition.kind === "user" || transition.persistDefault) && (this.supportsThinking() || effectiveLevel !== "off")) {
                 this.settingsManager.setDefaultThinkingLevel(effectiveLevel);
             }
             this._emit({ type: "thinking_level_changed", level: effectiveLevel });
diff -ruN a/dist/core/agent-session-services.d.ts b/dist/core/agent-session-services.d.ts
--- a/dist/core/agent-session-services.d.ts	1970-01-01 00:00:01.000000000 +0000
+++ b/dist/core/agent-session-services.d.ts	2026-08-12 10:01:03.174114060 +0000
@@ -45,6 +45,10 @@
     sessionStartEvent?: SessionStartEvent;
     model?: Model<any>;
     thinkingLevel?: ThinkingLevel;
+    thinkingPreference?: {
+        kind: "pinned";
+        level: ThinkingLevel;
+    };
     scopedModels?: Array<{
         model: Model<any>;
         thinkingLevel?: ThinkingLevel;
diff -ruN a/dist/core/agent-session-services.js b/dist/core/agent-session-services.js
--- a/dist/core/agent-session-services.js	1970-01-01 00:00:01.000000000 +0000
+++ b/dist/core/agent-session-services.js	2026-08-12 10:01:11.560570973 +0000
@@ -122,6 +122,7 @@
         sessionManager: options.sessionManager,
         model: options.model,
         thinkingLevel: options.thinkingLevel,
+        thinkingPreference: options.thinkingPreference,
         scopedModels: options.scopedModels,
         tools: options.tools,
         excludeTools: options.excludeTools,
diff -ruN a/dist/core/model-config.d.ts b/dist/core/model-config.d.ts
--- a/dist/core/model-config.d.ts	1970-01-01 00:00:01.000000000 +0000
+++ b/dist/core/model-config.d.ts	2026-08-12 09:59:58.141748974 +0000
@@ -6,6 +6,7 @@
     api: Type.TOptional<Type.TString>;
     baseUrl: Type.TOptional<Type.TString>;
     reasoning: Type.TOptional<Type.TBoolean>;
+    defaultThinkingLevel: Type.TOptional<Type.TUnion<[Type.TLiteral<"off">, Type.TLiteral<"minimal">, Type.TLiteral<"low">, Type.TLiteral<"medium">, Type.TLiteral<"high">, Type.TLiteral<"xhigh">, Type.TLiteral<"max">]>>;
     thinkingLevelMap: Type.TOptional<Type.TObject<{
         off: Type.TOptional<Type.TUnion<[Type.TString, Type.TNull]>>;
         minimal: Type.TOptional<Type.TUnion<[Type.TString, Type.TNull]>>;
diff -ruN a/dist/core/model-config.js b/dist/core/model-config.js
--- a/dist/core/model-config.js	1970-01-01 00:00:01.000000000 +0000
+++ b/dist/core/model-config.js	2026-08-12 09:59:58.141944732 +0000
@@ -134,6 +134,15 @@
     api: Type.Optional(Type.String({ minLength: 1 })),
     baseUrl: Type.Optional(Type.String({ minLength: 1 })),
     reasoning: Type.Optional(Type.Boolean()),
+    defaultThinkingLevel: Type.Optional(Type.Union([
+        Type.Literal("off"),
+        Type.Literal("minimal"),
+        Type.Literal("low"),
+        Type.Literal("medium"),
+        Type.Literal("high"),
+        Type.Literal("xhigh"),
+        Type.Literal("max"),
+    ])),
     thinkingLevelMap: Type.Optional(ThinkingLevelMapSchema),
     input: Type.Optional(Type.Array(Type.Union([Type.Literal("text"), Type.Literal("image")]))),
     cost: Type.Optional(ModelCostSchema),
diff -ruN a/dist/core/provider-composer.js b/dist/core/provider-composer.js
--- a/dist/core/provider-composer.js	1970-01-01 00:00:01.000000000 +0000
+++ b/dist/core/provider-composer.js	2026-08-12 09:59:58.142107886 +0000
@@ -63,6 +63,7 @@
         provider: providerId,
         baseUrl,
         reasoning: definition.reasoning ?? false,
+        defaultThinkingLevel: definition.defaultThinkingLevel,
         thinkingLevelMap: definition.thinkingLevelMap,
         input: (definition.input ?? ["text"]),
         cost: definition.cost ?? { input: 0, output: 0, cacheRead: 0, cacheWrite: 0 },
diff -ruN a/dist/core/sdk.d.ts b/dist/core/sdk.d.ts
--- a/dist/core/sdk.d.ts	1970-01-01 00:00:01.000000000 +0000
+++ b/dist/core/sdk.d.ts	2026-08-12 10:01:22.586171403 +0000
@@ -16,8 +16,13 @@
     modelRuntime?: ModelRuntime;
     /** Model to use. Default: from settings, else first available */
     model?: Model<any>;
-    /** Thinking level. Default: from settings, else 'medium' (clamped to model capabilities) */
+    /** Thinking level. Default: model-local default, then settings, else 'medium' (clamped to model capabilities) */
     thinkingLevel?: ThinkingLevel;
+    /** Explicit unclamped session thinking preference across model switches. */
+    thinkingPreference?: {
+        kind: "pinned";
+        level: ThinkingLevel;
+    };
     /** Models available for cycling (Ctrl+P in interactive mode) */
     scopedModels?: Array<{
         model: Model<any>;
diff -ruN a/dist/core/sdk.js b/dist/core/sdk.js
--- a/dist/core/sdk.js	1970-01-01 00:00:01.000000000 +0000
+++ b/dist/core/sdk.js	2026-08-12 10:01:37.133963184 +0000
@@ -111,6 +111,7 @@
             modelFallbackMessage += `. Using ${model.provider}/${model.id}`;
         }
     }
+    const preferredThinkingLevel = settingsManager.getDefaultThinkingLevel() ?? DEFAULT_THINKING_LEVEL;
     let thinkingLevel = options.thinkingLevel;
     // If session has data, restore thinking level from it
     if (thinkingLevel === undefined && hasExistingSession) {
@@ -120,7 +121,7 @@
     }
     // Fall back to settings default
     if (thinkingLevel === undefined) {
-        thinkingLevel = settingsManager.getDefaultThinkingLevel() ?? DEFAULT_THINKING_LEVEL;
+        thinkingLevel = model?.defaultThinkingLevel ?? settingsManager.getDefaultThinkingLevel() ?? DEFAULT_THINKING_LEVEL;
     }
     // Clamp to model capabilities
     if (!model) {
@@ -129,6 +130,7 @@
     else {
         thinkingLevel = clampThinkingLevel(model, thinkingLevel);
     }
+    const thinkingPreference = options.thinkingPreference;
     const defaultActiveToolNames = ["read", "bash", "edit", "write"];
     const allowedToolNames = options.tools ?? (options.noTools === "all" ? [] : undefined);
     const excludedToolNames = options.excludeTools;
@@ -248,6 +250,8 @@
         settingsManager,
         cwd,
         scopedModels: options.scopedModels,
+        thinkingPreference: thinkingPreference ?? { kind: "baseline", level: preferredThinkingLevel },
+        preferredThinkingLevel: thinkingPreference?.level ?? preferredThinkingLevel,
         resourceLoader,
         customTools: options.customTools,
         modelRuntime,
diff -ruN a/dist/main.js b/dist/main.js
--- a/dist/main.js	1970-01-01 00:00:01.000000000 +0000
+++ b/dist/main.js	2026-08-12 10:01:52.270786483 +0000
@@ -365,6 +365,10 @@
     if (parsed.thinking) {
         options.thinkingLevel = parsed.thinking;
     }
+    // Preserve an explicit CLI/model-suffix level as a session preference across
+    // model switches. Scoped model pins remain specific to their model.
+    const pinnedThinkingLevel = parsed.thinking ?? (cliThinkingFromModel ? options.thinkingLevel : undefined);
+    options.thinkingPreference = pinnedThinkingLevel === undefined ? undefined : { kind: "pinned", level: pinnedThinkingLevel };
     // Scoped models for Ctrl+P cycling
     // Keep thinking level undefined when not explicitly set in the model pattern.
     // Undefined means "inherit current session thinking level" during cycling.
@@ -616,6 +620,7 @@
             sessionStartEvent,
             model: sessionOptions.model,
             thinkingLevel: sessionOptions.thinkingLevel,
+            thinkingPreference: sessionOptions.thinkingPreference,
             scopedModels: sessionOptions.scopedModels,
             tools: sessionOptions.tools,
             excludeTools: sessionOptions.excludeTools,
@@ -624,7 +629,7 @@
         });
         const cliThinkingOverride = parsed.thinking !== undefined || cliThinkingFromModel;
         if (created.session.model && cliThinkingOverride) {
-            created.session.setThinkingLevel(created.session.thinkingLevel);
+            created.session.setThinkingLevel(sessionOptions.thinkingPreference?.level ?? created.session.thinkingLevel);
         }
         return {
             ...created,
